console.log('hey im attaches the file example the file is run  is server.js bro');

//  create the variable that use in server file
var age = 21;

const addnum = (a,b) =>
    {
        return a+b;
    }
// acess the server use this method
module.exports = {
    age,
    addnum
}